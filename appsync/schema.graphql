type Mutation {
    createChatToken(deviceId: ID!, token: ID!): ID!
}

type Query {
    verifyToken(token: ID!): TokenInfo!
}

type Schema {
    query: Query
    mutation: Mutation
}

type TokenInfo {
    identity: ID!
    contexts: [String!]!
}