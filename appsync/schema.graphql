type ChatToken {
    identity: ID!
    jwt: ID!
}

input ToolboxTokenInput {
    jwt: ID!
}

type Mutation {
    createChatToken(deviceId: ID!, toolboxToken: ToolboxTokenInput): ChatToken!
}

type Query {
    _: Boolean
}

type Schema {
    query: Query
    mutation: Mutation
}
